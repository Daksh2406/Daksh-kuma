document.addEventListener('DOMContentLoaded', () => {
    // 1. Project Carousel Scrolling
    const carousel = document.getElementById('works-carousel');
    // Note: Carousel navigation buttons are not implemented in HTML for simplicity, 
    // but the scrolling logic is ready if you add them.
    
    if (carousel) {
        // Set up scrolling for touch/swipe/mouse drag gestures (optional)
        // You can add buttons (prev/next) and use the logic below:
        
        // Example for button scrolling (requires adding buttons in HTML)
        /*
        const scrollAmount = 430; // Card width (400px) + gap (30px)
        
        document.querySelector('.next-btn').addEventListener('click', () => {
             carousel.scrollLeft += scrollAmount;
        });

        document.querySelector('.prev-btn').addEventListener('click', () => {
             carousel.scrollLeft -= scrollAmount;
        });
        */
    }
});